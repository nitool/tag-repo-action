name: 'Auto tag'
description: 'Auto tagging your repository based on Gitflow branch names'
inputs:
  githubToken:
    description: 'Github token, pass {{ secret.GITHUB_TOKEN }}'
    required: true
runs:
  using: 'node12'
  main: 'index.js'
